get:
  summary: Get current user's favorites
  tags:
    - Users
  security:
  - bearerAuth: []
  # TODO: more responses
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/userId.yaml
